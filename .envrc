#!/bin/bash
[ -f .env ] && source .env

# export S3_INPUT_PATH="s3://nesta-ds-projects/<MISSION OR PRACTICE>/<PROJECT NAME>"

# Activate the conda environment
eval "$(conda shell.bash activate "asf_levies_model_app")"
[ "$CONDA_DEFAULT_ENV" = "asf_levies_model_app" ] || echo "Have you run \`make install\`?"
